# R4Psy 课程 Agent 示例指令书（问卷类）

## 使用规则

```text
# 你将收到一个问卷数据处理任务。先读取并遵守下面所有约束，再开始回答。
# 把方括号中的占位符视为调用者随后会补充的任务上下文；不要自行脑补缺失信息。
# 正文只保留你需要执行或遵守的指令；补充说明只保留在注释里。
# 包、路径、变量名优先与课件保持一致；不要擅自更换包、改写路径或重命名变量。
# 默认流程：先澄清关键前提，再给代码；如果任务是多步骤，先给计划，再分步实现。
```

---

## 1. 问卷数据清洗与量表计分

```text
# 适用场景：单个数据框；列主要是人口学变量、题项、量表分数。
# 推荐包：bruceR + tidyverse（重点是 dplyr / tidyr / ggplot2）。
# 路径写法优先使用 here::here("slides", "data", ...)。

你现在要协助处理一份问卷数据。先进入 Planning 模式，不要直接写代码。

已知数据：
- 文件路径：[here::here(...) 或已加载的数据框名]
- 关键变量：[例如 Site, Sex, age, ALEX1:ALEX16, DEQ]

目标：
1. 选择需要的列；
2. 检查并修正变量类型；
3. 处理缺失值；
4. 如果有反向题，完成反向计分；
5. 计算总分或均分；
6. 输出按组汇总结果。

执行约束：
- 请使用 bruceR + tidyverse；
- 不要擅自新增课件里没有的包；
- 每一步都加注释；
- 如果有前提不清楚，请先追问；
- 在计划确认前，不要直接给完整代码。
```

## 2. 问卷数据分组汇总

```text
# 适用场景：已经完成基本清洗，只需要按组输出描述统计。
# 课件中常见操作：group_by() + summarise()；总分/均分通常用 rowSums() / rowMeans() 或 bruceR::SUM() / MEAN()。

你将接收一个已经完成基本清洗的问卷数据框 [数据框名]。

请只完成下面这个小任务，不要扩展到后续分析：
1. 按 [分组变量] 分组；
2. 计算 [目标变量] 的 [均值 / 标准差 / 样本量 / 中位数]；
3. 输出一个整洁的数据框。

执行约束：
- 请使用 dplyr；
- 先说明输入和输出；
- 再给代码；
- 如果缺失值处理会影响结果，请先指出。
```

## 3. 问卷类 EDA 与可视化

```text
# 适用场景：单表问卷数据的分布、组间差异、变量关系。
# 推荐包：ggplot2（tidyverse 内）+ bruceR（如果需要课件风格主题）。

你现在要对问卷数据做 EDA / 可视化。

已知数据：
- 数据框：[数据框名]
- x 变量：[变量名 + 类型]
- y 变量：[变量名 + 类型；如果没有就写“无”]
- 分组变量：[变量名 + 类型；如果没有就写“无”]

目标：
- 请先判断最合适的图形类型；
- 再给一个基础的 ggplot2 版本；
- 如果需要，再补上标题、坐标轴标签、颜色映射。

执行约束：
- 先解释为什么选这类图；
- 再给代码；
- 不要一次给多个完全不同风格的版本；
- 如果图形不适合当前变量类型，请直接指出。
```

## 4. 问卷类 ggplot / 数据探索报错排查

```text
# 适用场景：报错来自数据类型、缺失值、aes() 映射、分组变量、管道前一步输出。

你现在要排查一段问卷数据 EDA / ggplot 代码中的报错。

当前代码：
[粘贴最小可复现代码]

报错信息：
[原样粘贴]

变量信息：
- 数据框：[数据框名]
- 变量 1：[变量名 + 类型]
- 变量 2：[变量名 + 类型]
- 分组变量：[变量名 + 类型；如果没有就写“无”]

预期结果：
[预期图形或结果]

请按下面顺序处理：
1. 先判断最可能出错的是哪一层；
2. 再解释这条报错通常意味着什么；
3. 给出最小修改方案；
4. 最后告诉我怎样验证修复后的结果是否合理。

请不要一开始就重写整段代码。
```

---

## 提交前自检

```text
# 1. 路径是否与仓库一致（优先 here::here("slides", "data", ...)）。
# 2. 包是否与课件一致（问卷优先 bruceR + tidyverse）。
# 3. 变量名、分组变量、缺失值处理要求是否已经明确写入任务上下文。
# 4. 是否要求你先澄清再写代码；多步骤任务是否要求你先给计划。
# 5. 是否把解释性内容压缩到注释中，而不是散落在正文。
```

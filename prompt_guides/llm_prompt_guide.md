# R4Psy 课程 LLM Prompt 指令书

## 使用规则

```text
# 直接替换方括号中的占位符后使用。
# 正文只保留可执行指令；补充说明只写在注释里。
# 包、路径、变量名优先与课件保持一致；不要让模型擅自改包或改路径。
# 默认要求：先澄清关键前提，再给代码；如果任务是多步骤，先给计划，再分步实现。
```

---

## 一、问卷类指令

### 1. 问卷数据清洗与量表计分

```text
# 适用场景：单个数据框；列主要是人口学变量、题项、量表分数。
# 推荐包：bruceR + tidyverse（重点是 dplyr / tidyr / ggplot2）。
# 路径写法优先使用 here::here("slides", "data", ...)。

我正在处理问卷数据。请先进入 Planning 模式，不要直接写代码。

我的数据：
- 文件路径：[here::here(...) 或已加载的数据框名]
- 关键变量：[例如 Site, Sex, age, ALEX1:ALEX16, DEQ]

我的目标：
1. 选择需要的列；
2. 检查并修正变量类型；
3. 处理缺失值；
4. 如果有反向题，完成反向计分；
5. 计算总分或均分；
6. 输出按组汇总结果。

约束：
- 请使用 bruceR + tidyverse；
- 不要擅自新增课件里没有的包；
- 每一步都加注释；
- 如果有前提不清楚，请先追问；
- 在计划确认前，不要直接给完整代码。
```

### 2. 问卷数据分组汇总

```text
# 适用场景：已经完成基本清洗，只需要按组输出描述统计。
# 课件中常见操作：group_by() + summarise()；总分/均分通常用 rowSums() / rowMeans() 或 bruceR::SUM() / MEAN()。

我已经有一个清洗后的问卷数据框 [数据框名]。

请只完成下面这个小任务：
1. 按 [分组变量] 分组；
2. 计算 [目标变量] 的 [均值 / 标准差 / 样本量 / 中位数]；
3. 输出一个整洁的数据框。

约束：
- 请使用 dplyr；
- 先说明输入和输出；
- 再给代码；
- 如果缺失值处理会影响结果，请先指出。
```

### 3. 问卷类 EDA 与可视化

```text
# 适用场景：单表问卷数据的分布、组间差异、变量关系。
# 推荐包：ggplot2（tidyverse 内）+ bruceR（如果需要课件风格主题）。

我正在对问卷数据做 EDA / 可视化。

数据：
- 数据框：[数据框名]
- x 变量：[变量名 + 类型]
- y 变量：[变量名 + 类型；如果没有就写“无”]
- 分组变量：[变量名 + 类型；如果没有就写“无”]

目标：
- 请先判断最合适的图形类型；
- 再给一个基础的 ggplot2 版本；
- 如果需要，再补上标题、坐标轴标签、颜色映射。

约束：
- 先解释为什么选这类图；
- 再给代码；
- 不要一次给多个完全不同风格的版本；
- 如果图形不适合当前变量类型，请直接指出。
```

### 4. 问卷类 ggplot / 数据探索报错排查

```text
# 适用场景：报错来自数据类型、缺失值、aes() 映射、分组变量、管道前一步输出。

我在问卷数据的 EDA / ggplot 代码里遇到报错。

当前代码：
[粘贴最小可复现代码]

报错信息：
[原样粘贴]

变量信息：
- 数据框：[数据框名]
- 变量 1：[变量名 + 类型]
- 变量 2：[变量名 + 类型]
- 分组变量：[变量名 + 类型；如果没有就写“无”]

我原本想得到的结果：
[预期图形或结果]

请按下面顺序处理：
1. 先判断最可能出错的是哪一层；
2. 再解释这条报错通常意味着什么；
3. 给出最小修改方案；
4. 最后告诉我怎样验证修复后的结果是否合理。

请不要一开始就重写整段代码。
```

---

## 二、实验类指令

### 1. 批量导入与清洗实验数据

```text
# 适用场景：多个 .out / .csv 文件；试次级数据；需要先合并，再清洗。
# 课件中常见操作：list.files()、read.table()、lapply()/for loop、bind_rows()、filter()、drop_na()。
# 推荐包：tidyverse；如果已有课件路径，优先保持 here::here("slides", "data", "match", ...)。

我正在处理实验数据。请先进入 Planning 模式，不要直接写代码。

我的数据：
- 文件目录：[here::here(...) 或实际目录]
- 文件格式：[.out / .csv / 其他]
- 关键变量：[例如 Sub, Shape, Match, ACC, RT]

我的目标：
1. 批量读取所有文件；
2. 合并为一个数据框；
3. 统一变量类型；
4. 排除无效试次；
5. 输出清洗后的试次级数据。

约束：
- 请使用 tidyverse；
- 如果需要 read.table() / list.files() / bind_rows()，请优先沿用课件写法；
- 每一步都加注释；
- 先追问筛选标准（例如 ACC、RT、Hand、Date 等）再写代码；
- 在计划确认前，不要直接给完整实现。
```

### 2. 分条件汇总、长宽转换与效应指标

```text
# 适用场景：已经拿到清洗后的试次级数据，需要按被试与条件汇总，再转宽表并计算指标。
# 课件中常见指标：mean_RT、mean_ACC、SPE；常见函数：group_by()、summarise()、extract()、pivot_wider()、mutate()。

我已经有一个清洗后的实验数据框 [数据框名]。

请只完成下面这些步骤：
1. 按 [被试变量] 和 [条件变量] 计算 [mean_RT / mean_ACC / 其他指标]；
2. 如果需要，从 [原变量名] 中拆出 [新变量名 1] 和 [新变量名 2]；
3. 将长数据转成宽数据；
4. 计算 [目标效应指标，例如 moral_SPE]。

约束：
- 请使用 tidyverse；
- 先说明每一步的输入和输出；
- 再给代码；
- 如果某一步依赖上一部的数据结构，请明确写出来；
- 不要越级继续做未要求的后续分析。
```

### 3. 实验类 EDA 与可视化

```text
# 适用场景：试次级或按条件汇总后的实验数据；关注 RT、ACC、条件差异、被试差异。
# 推荐包：ggplot2（tidyverse 内）；如果需要课件风格，可补 bruceR::theme_bruce()。

我正在对实验数据做 EDA / 可视化。

数据：
- 数据框：[数据框名]
- x 变量：[变量名 + 类型]
- y 变量：[变量名 + 类型]
- 分组变量：[变量名 + 类型；如果没有就写“无”]
- 数据层级：[试次级 / 被试汇总级 / 条件汇总级]

目标：
- 请先判断最合适的图形类型；
- 再给一个基础的 ggplot2 版本；
- 如果当前图不足以支持结论，请直接指出。

约束：
- 先解释为什么选这类图；
- 再给代码；
- 如果需要先聚合再画图，请明确说明；
- 不要把试次级数据和汇总级数据混在同一层解释。
```

### 4. 实验类报错排查

```text
# 适用场景：批量读取失败、列名不一致、类型转换失败、group_by/summarise 输出不对、pivot_wider 后结构异常。

我在实验数据处理流程中遇到报错。

当前代码：
[粘贴最小可复现代码]

报错信息：
[原样粘贴]

数据与任务信息：
- 文件来源：[目录或文件名模式]
- 当前数据框：[数据框名]
- 关键变量：[变量名列表]
- 我当前所在步骤：[批量读取 / 清洗 / 汇总 / 拆分变量 / 转宽 / 计算指标]
- 我原本想得到的结果：[预期输出]

请按下面顺序处理：
1. 先判断错误更可能来自哪一步；
2. 再解释报错含义；
3. 给出最小修复方案；
4. 告诉我修复后应该检查什么结果。

请不要直接重写整套分析流程。
```

### 5. 使用 `Analysis_Notes.md` 约束实验分析逻辑

```text
# 适用场景：实验分析步骤较长，包含筛选规则、变量定义、计算顺序、长宽转换规则。
# 推荐做法：如果沿用课件示例文件名，先把研究问题、数据来源、关键变量、计算逻辑写进 Analysis_Notes.md，再让模型继续当前小任务。

Please read Analysis_Notes.md first.

This file defines my research question, data source, key variables, and analysis logic.
Based on this file, help me continue the current R analysis.

Current task:
[只写当前这一步，例如“按被试和条件计算平均 RT”]

Requirements:
- Only complete this step;
- Explain the input and expected output first;
- Then give the code;
- If Analysis_Notes.md is ambiguous or inconsistent, point it out before coding.
```

---

## 三、提交前自检

```text
# 1. 路径是否与仓库一致（优先 here::here("slides", "data", ...)）。
# 2. 包是否与课件一致（问卷优先 bruceR + tidyverse；实验优先 tidyverse，必要时沿用课件中的 read.table / list.files / bind_rows / pivot_wider）。
# 3. 变量名、条件名、筛选标准是否已经明确写入 prompt。
# 4. 是否要求模型先澄清再写代码；多步骤任务是否要求先给计划。
# 5. 是否把解释性内容压缩到注释中，而不是散落在正文。
```
